1. 包已经包含vs 解决方案项目。打开。修改下配置路径。就可以打开项目；地址：D:\shoujiduan2\shoujiduan2\win32\game517.xxx
2.修改客户访问服务端地址。
  2.1 client\client\src\plaza\modul\yl.lua. 共 3个地方的ip.其中一个是http协议。访问webservice.
  2.2 client\base\src\app\view\welcomescene.lua 一个地方，也是http协议,访问webservice .目的好像是获得游戏列表。


3.多开账号测试。可以注册。可以多开的。atl+4. 非debug可以开多个。要测试。可以开几个no debug.和开一个debug.应该是同一IP原因？
没有关系。同一ip.游戏有设置是否同一 ip，认为作弊。
4.debug下的账号，可以打断点。可以用来跟踪。但是最终是要写到日志，来跟踪的。

在不? 
5.写日志。因为这个插件和vs 配合的不好。导致lua的print出不来。这个本来是可以在cocos的cmd窗口出来。但是现在没有cmd窗口了。
不过可以打断点。也就无所谓了。coocs  ide的是不能打断点的。vs可以。

6.写日志。就自己用lua 的 语法。写到记事本。望湖自己写了一个函数。但是没有去看，自己写了一个。
   这些软件都要删掉。因为特殊情况下。可能要抓包。或者查看，开了多少socket. 点进去游戏。和没点进去。
   就发现多了2条tcp .那么在跟踪的时候。不能确定是否是长连接。这个时候查看socket。就可以知道点了子游戏
   才会建立长连接。其他是http.可以帮助你跟踪。所以一些网络软件，删除比较好。

7.那个是sniff.抓包的。还是要熟悉cmd。命令。
8.https://59.110.157.35:8080/svn/doc 。zhangjing 123456
9.你的客户端是原始包。好像有bug,我传的是我本机的。但是我又打了一些测试代码。
10.日志的方法在client\base\src\mylog.lua.使用方法。 require("base.src.mylog"):_debugT("hi")
    "hi" ，里面的日志信息。要你自己去写。
11.日志的位置在 client\linsonlog.txt
12.打日志：
    1.main.lua。程序入口。先打断点。比如第一步，必须先看看程序启动的流程。
    2.f10.行断点进去。
    3.突然就跑到其他地方去了。是行跟的。因为是MVC模式。
    4.奇怪 。没有进入APP。他是自己跟棕。还是要手动的，手动按。F10.行跟踪。F11.进入跟踪。所以突然
      根丢，必须按F11.耐心看代码。
    5.MVC。模式。所以，你看了代码会发现。进入的是WELCOMESCENE。这个是登录页面前的页面。
    6.大概这里就可以看出来。是判断本地是否 已经安装 软件。没有安装，就解压。否则测试 本地和服务器的版本 是否 一直
      是否 需要更新。继续跟。
    7.这种无源代码 的。表示可能是操作系统。或者COCOS。反正就不是你的代码。那么按SHIFT+F11.调处 。
    8.还要注意看堆栈。一看。大概知道是MVC调用 了。看看函数。可能是COCOS的系统函数。所以只能反汇编。
    9.又跟丢。要查下，为什么。


13.需要做的工作。
   1.因为打日志会搞错数据，所以必须改进日志函数，不然很浪费 时间查BUG。
   2.所有自己写的代码，必须写自己名字 @ZHANGJING.这样好查.

   可以了不?知道如何 改那个函数不?知道了。我等下试着打几个。先休息一下.

	